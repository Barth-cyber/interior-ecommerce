name: GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    concurrency:
      group: pages-deploy
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact for GitHub Pages
      - name: Upload artifact for GitHub Pages
  uses: actions/upload-pages-artifact), update any usage of actions/upload-artifact@v3 to at least actions/upload-artifact@v4.

Example:
```yaml
- uses: actions/upload-artifact@v4

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
